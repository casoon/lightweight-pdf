/plan/
/target
*.wasm
!assets/**/*.wasm
.DS_Store
# cargo-judge output — regenerated on demand, never meant to be committed.
/.judge/
# Output of `cargo run --example invoice`/`report` — written to the repo
# root, never meant to be committed. examples/demo_*.pdf are tracked on
# purpose (checked-in sample output).
/*.pdf
# lightweight-pdf-testing writes these next to a reference snapshot only
# on a failed comparison — transient, never meant to be committed.
test-fixtures/snapshots/*.diff.png
